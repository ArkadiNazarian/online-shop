export const routes = () => {

    const default_path = "*";
    const account_path = "/account";
    const products_path = "/products";
    const sigin_path="/account/signin"

    return {
        default_path,
        account_path,
        products_path,
        sigin_path
    }
}