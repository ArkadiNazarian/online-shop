export const route_names = () => {
    const signin_path = "/auth/signin";
    const signup_path = "/auth/signup";
    const default_path = "*";
    return {
        signin_path,
        signup_path,
        default_path
    }
}